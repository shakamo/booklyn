box: phusion/passenger-ruby22
dev:
  steps:
    - bundle-install
    - internal/watch:
      code: |
        bundle exec unicorn -p 8080 -c ./config/unicorn.rb
      reload: true
build:
  steps:
    - bundle-install
    - script:
      name: minitest
      code: bin/rake test
